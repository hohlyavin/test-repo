name: Hello World

on:
  push:
    branches: [ "main" ]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Greeting from gh workflow
        run: echo "Hello World"
      - name: Second step
        run: id
      - name: Third step
        run: ls -l
